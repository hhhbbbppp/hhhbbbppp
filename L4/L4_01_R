#include <iostream> // cout cin
#include <fstream>  // ifstream ofstream
using namespace std;

int main() {
	ifstream GG("gg.txt");
	if (!GG) {
		cout << " no file ";
		return 444;
	}
	int a, b, c;
	char d[5], e[5];

	for (int b = 1; b < 20; b++) {
		GG >> a >> d >> b >> e >> c;
		cout << a << " " << d << " " << b << " " << e << " " << c << endl;
	}
	return 777;
}
